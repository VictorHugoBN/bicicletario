import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#620682',
    },
    secondary: {
      main: '#FFE500',
    },
  },
  typography: {
    fontFamily: 'Amaranth',
  },
});

import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#160054',
      light: '#28009A',
    },
    secondary: {
      main: '#CD6A00',
      light: '#CD8702',
    },
  },
  typography: {
    fontFamily: 'Amaranth',
  },
});
